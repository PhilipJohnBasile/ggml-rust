[package]
name = "llama-runtime"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
readme = "README.md"
repository.workspace = true
description = "Llama configuration and GGUF layout validation for a Rust-native runtime"

[dependencies]
ggml-model = { version = "0.1.0", path = "../model" }
ggml-tensor = { version = "0.1.0", path = "../tensor" }

[lints]
workspace = true
